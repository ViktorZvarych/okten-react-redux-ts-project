import {MovieInfo, PopularList, UpcomingList} from "../../components";

const MovieInfoPage = () => {
    return (
        <div>
            <MovieInfo/>

            <PopularList/>
            <UpcomingList/>
        </div>
    );
};

export {MovieInfoPage};